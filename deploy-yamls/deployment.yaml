apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    description: Lykke.Service.Campaign
  labels:
    app: campaign
    environment: Development
    tier: Service
  name: campaign
  namespace: services
spec:
  revisionHistoryLimit: 1
  minReadySeconds: 10
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  replicas: 1
  selector:
    matchLabels:
      app: campaign
  template:
    metadata:
      labels:
        app: campaign
      name: campaign
    spec:
 #     nodeSelector:
 #       type: 
 #     tolerations:
 #     - effect: NoSchedule
 #       operator: Exists
      imagePullSecrets:
      - name: campaign-docker
      containers:
      - name: campaign
        image: lykkebusiness/service-campaign:dev
        imagePullPolicy: Always
        lifecycle:
          postStart:
            exec:
             command:
             - /bin/sh
             - -c 
             - "/bin/echo 'options single-request-reopen' >> /etc/resolv.conf"
        ports:
        - containerPort: 5000
        readinessProbe:
          httpGet:
            path: /api/isalive
            port: 5000
          initialDelaySeconds: 40
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /api/isalive
            port: 5000
          initialDelaySeconds: 40
          periodSeconds: 20
        resources:
          requests:
            memory: 128Mi 
            cpu: 40m 
          limits:
            memory: 1024Mi 
            cpu: 1000m 
        env:
        - name: BuildNumber
          value: '$version'
        - name: SettingsUrl
          value: '$ConnectionString'
        - name: ENV_INFO
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: MyMonitoringUrl
          value: http://campaign.services.svc.cluster.local/api/isalive
