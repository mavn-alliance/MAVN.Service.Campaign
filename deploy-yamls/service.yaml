apiVersion: v1
kind: Service
metadata:
  labels:
    app: campaign
    environment: Development
  name: campaign
  namespace: services
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 5000
  selector:
    app: campaign
  sessionAffinity: None
  type: ClusterIP
